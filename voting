include<iostream>
using namespace std;
int main() {

    int age;
    cout << "Enter age of a user:";
    cin>>age;

    if (age >= 18) {
        cout << "You are eligible for voting";
    } else {
        cout << "You are not eligible for voting";
    }

    return 0;
}


output:
Enter age of a user:15
You are not eligible for voting

Enter age of a user:45
You are eligible for voting
